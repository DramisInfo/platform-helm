Always validate tasks with latest documentation using context7 mcp tools before providing answers or generating code.

Always use kubernetes tool when asked to monitor or investigate kubernetes clusters.

Never commit changes to git.  The user will handle that.