apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "platform-core.fullname" . }}-test-connection"
  labels:
    {{- include "platform-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "platform-core.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
